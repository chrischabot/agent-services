---
description: Render a wiki-grounded report draft from already-collected evidence
argument-hint: [topic]
---

# Research Brief

The user invoked this command with: $ARGUMENTS

Use `research_brief_from_wiki` only as a report/summary artifact renderer over already-collected local wiki pages and source cards. This is not a quick research mode. If local sources are weak, say what is missing and recommend running `$deep-research` before treating the artifact as complete.
