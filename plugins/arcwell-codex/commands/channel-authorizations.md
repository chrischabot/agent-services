---
description: List channel authorization policy entries.
---

List Arcwell channel authorization policy with `channel_authorizations`.

Call out subjects that can write project state, because those subjects can bind incoming channel messages to projects.
