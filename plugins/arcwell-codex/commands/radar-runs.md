---
description: List radar runs
argument-hint: [optional-filter]
---

# Radar Runs

The user invoked this command with: $ARGUMENTS

Use `radar_runs`. Summarize status, stage, counts, and errors. Distinguish
local source-card proof from production-data proof.
