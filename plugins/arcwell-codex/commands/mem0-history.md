---
description: Show Arcwell Memory history for one id
argument-hint: MEMORY_ID
---

# Arcwell Memory History

The user invoked this command with: $ARGUMENTS

Use `mem0_history` for the supplied id. Report adds, updates, and deletes
concisely without exposing unrelated memories.
