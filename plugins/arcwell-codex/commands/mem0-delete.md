---
description: Delete one Arcwell Memory by id
argument-hint: MEMORY_ID [USER_ID]
---

# Arcwell Memory Delete

The user invoked this command with: $ARGUMENTS

Use `mem0_delete` for the exact memory id. If the user gave a vague natural
language deletion request, search first and confirm the exact memory id.
