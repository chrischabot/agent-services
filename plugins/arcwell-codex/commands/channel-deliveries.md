---
description: Inspect channel delivery attempts and retry hints.
---

List Arcwell channel delivery attempts with `channel_delivery_list`.

If the user gives a message id, pass it as `message_id`. Otherwise list recent attempts and summarize failed deliveries, provider status, error, and retry time.
