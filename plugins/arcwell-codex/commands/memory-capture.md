---
description: Capture text into Arcwell Memory review candidates
argument-hint: TEXT
---

# Memory Capture

The user invoked this command with: $ARGUMENTS

Use `memory_capture` in review mode by default. Only set `auto_apply` when the
user explicitly asks for automatic application or has configured that policy.
Sensitive facts must remain reviewable unless explicitly configured otherwise.
