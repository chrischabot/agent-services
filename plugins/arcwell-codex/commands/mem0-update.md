---
description: Update one Arcwell Memory by id
argument-hint: MEMORY_ID TEXT [USER_ID]
---

# Arcwell Memory Update

The user invoked this command with: $ARGUMENTS

Use `mem0_update` for the exact memory id and replacement text. If the id is
ambiguous, search first and ask for the exact target instead of guessing.
