---
description: Search personal memory
argument-hint: [query]
---

# Memory Search

The user invoked this command with: $ARGUMENTS

Use `mem0_search` first for the query. Use `memory_search` only as the
compatibility fallback. Summarize matching facts briefly and mention when no
relevant memory is found.
