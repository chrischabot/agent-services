# Arcwell Status Matrix

Last audited: 2026-06-20

This file is the project compass and todo list. It is intentionally blunt. A package
directory, README, command prompt, or MCP tool does not mean the capability is
finished. The status below is based on the current repository, not the product
vision.

## Legend

| Status | Meaning |
| --- | --- |
| Done | Implemented, documented, and covered by relevant tests for the current intended scope. |
| Partial | Usable slice exists, but important behavior is missing or shallow. |
| Scaffold | Boundary, docs, commands, or placeholder flow exists; product behavior is not real enough yet. |
| Missing | Not implemented in any meaningful form. |
| Risk | Exists but can mislead, lose data, leak trust boundaries, or fail silently. |

## Fresh Validation

| Check | Result | What it proves | What it does not prove |
| --- | --- | --- | --- |
| `cargo test --all --all-features` | Pass, full Rust/doc suite | Rust CLI/core compile; the in-repo Arcwell Memory crates compile and pass their inherited provider/vector/server tests; severe tests pass for backup restore drills including memory artifacts, strict doctor checks, service plist contract checks for corrupt metadata/missing binary, HTTP hardening, policy fail-closed behavior, LaunchAgent plist escaping, Telegram drain/auth/routing, explicit Telegram send authorization, Telegram send delivery recording, Telegram retry reuse, Telegram transport-error token redaction, cost budget blocking/reservation race prevention/retry-storm overspend prevention/scheduled kill-switch blocking, wiki URL redirect/content-type/size/prompt-injection handling, source-card canonical dedupe/schema validation/audit flags, research fake-citation/generated-recursion/contradiction/stale-source/SEO-spam/prompt-injection audit coverage, adapter cursor/source-health partial-write behavior, X token expiry/refresh failure redaction, X definitive watch-list failure safety, X quota/malformed/partial/duplicate cursor safety, X prompt-injection-as-evidence digest flow, secret lifecycle redaction/expiry/backup sensitivity, work-memory trace redaction/search/consolidation, procedure candidate extraction/apply/versioning/search/curation with adversarial trust-boundary coverage, Arcwell Memory lifecycle/forget behavior, provider-backed candidate update/delete, sensitive capture review, false-positive eval corpus, prompt-injection secret review, reviewable conflict updates, recall context, dream/reconcile, active-store forget cascade, and backup forget tombstones. | Live Telegram exact incoming channel-record assertion, Codex plugin install/hooks, Claude Desktop connection, Linux systemd service behavior, live adapter/provider API behavior including X user-context account-data scopes, live provider credential probes/revocation, live Codex/Claude work-run lifecycle/procedure retrieval, model-backed research synthesis/extraction, and historical-backup erasure for forgotten memories. |
| `cargo build -p arcwell` plus temporary-home CLI smoke | Pass | The regular `arcwell` executable builds, and memory capture/recall/hook-capture/dream/events run against a mock temporary home. | Real Codex hook execution, real model/provider extraction, and installed package behavior. |
| `scripts/telegram-live-smoke` | Partial live pass, exact incoming assertion blocked | Local auth pass, Telegram `getMe`, webhook setup to the Arcwell edge worker, and outgoing provider send passed. One Telegram webhook event reached the deployed edge and was acked, but its text did not match the exact expected smoke phrase, so the strict incoming channel-record assertion timed out. | Re-run with a known exact chat message and preserve the disposable `ARCWELL_HOME` for post-failure inspection if it fails again. |
| `scripts/x-live-smoke` | Partial live pass, then blocked | Local replay/source-card smoke passed. With the available bearer token, live X recent search reached the provider and returned count-shaped output. Definitive watch rebuild then failed safely with X `403 Unsupported Authentication` because bookmarks/follows require OAuth 1.0a User Context or OAuth 2.0 User Context, not application-only bearer auth. | Current-account bookmarks/follows and watched-source monitoring need a user-context token/scope. |
| `scripts/arcwell-dev smoke` + `scripts/arcwell-dev sync` | Pass | The generated dev plugin is materialized, the debug-binary wrapper works, JSON manifests parse, and the installed Codex plugin cache sync path works for `arcwell-codex-dev`. | Current already-running Codex thread does not automatically reload new tools/hooks; in-app slash command/hook smoke still requires a fresh thread or skill reload. |
| `scripts/service-live-smoke` | Pass | macOS user LaunchAgent install/status/restart/killed-worker recovery/logs/uninstall works against a real launchd service with disposable `HOME`/`ARCWELL_HOME`; strict doctor catches stale heartbeat, corrupt plist metadata, and missing worker binary; uninstall leaves unrelated Arcwell home data intact. | Linux systemd user-service behavior is explicitly deferred and unproven; Homebrew/release packaging is still unbuilt. |
| `scripts/release-readiness-smoke` | Pass | A candidate release binary and stable Codex plugin can be staged in disposable paths with spaces/hostile characters; the smoke catches stale `PATH` binaries, interrupted upgrade corruption, old schema drift, duplicate no-load service installs, bad log paths, unwritable homes, and uninstall cleanup that would delete user data. | It does not publish Homebrew/GitHub artifacts, prove signed checksums, run Linux systemd, or prove fresh-thread Codex app command/hook behavior. |
| `scripts/verify-codex-plugin-docs` | Pass, 99 commands / 11 skills / 108 MCP tools / 125 docs-prompts checked | Slash prompt files, the command catalog, skill directories, MCP tool registrations, tracked CLI-only references, untrusted source/channel prompt guards, and package README status badges agree. | It does not prove every slash command works in a fresh Codex thread or that live Telegram/Cloudflare/Claude/X integrations are operational. |
| `scripts/claude-mcp-smoke` | Pass locally; host config blocked | `arcwell mcp` starts as a Claude-style stdio subprocess, negotiates capabilities, lists expected tools/resources, serves representative resources/tools, returns explicit errors for malformed frames, unsupported methods, unknown resources, and bad `ARCWELL_HOME`, handles a bounded 256 KiB resource response without truncation/amplification, falls back to `HOME/.arcwell` when `ARCWELL_HOME` is unset, and reports Claude lifecycle/thread hooks as unavailable. | The local Claude Desktop config did not define `mcpServers.arcwell`, so an authenticated Claude Desktop/Code UI session listing or calling Arcwell tools is not proven. |
| `npm run typecheck` in `packages/arcwell-edge-inbox/worker` | Pass | Worker TypeScript typechecks. | Runtime behavior on deployed Cloudflare. |
| `npm test` in `packages/arcwell-edge-inbox/worker` | Pass, 13 tests | In-memory worker store covers unauthorized/oversized input, secret rotation, replay-storm and Telegram chat rate limits, idempotency, lease, short staging leases, ack, nack retry delay/dead-letter, expiry, Telegram webhook normalization, and malformed Telegram rejection; a D1 fake-store regression covers deployed SQL lease candidate selection. | Live Telegram webhook remains unproven. |
| `npm test` in `packages/arcwell-email` | Pass, 11 tests | The email mapper fixtures refute spoofed display `From:` authorization, malicious HTML/script/CSS promotion, prompt-injection-as-instructions, attachment bombs, tracking-link fetching assumptions, duplicate `Message-ID` replay, oversized bodies, auto-responder loops, and unauthorized routes. | No Cloudflare Email Routing Worker, raw MIME parser, local Rust email drain, SQLite persistence, Gmail polling, or outbound email/digest delivery is implemented. |
| `npx wrangler deploy` + deployed `/health` and forged `/events` smoke | Pass | Worker deployed to `https://arcwell-edge-inbox.chabotc.workers.dev`; deployed health reports durable `ok: true`; forged edge secret returns `401`. Latest live-smoked version: `df79f7e0-574c-483e-8cf8-a67ba95bae12`. | Live Telegram webhook remains separate. |
| `npx wrangler d1 execute arcwell_edge_inbox --remote --command "SELECT 1 AS ok"` | Pass | Cloudflare credentials can reach the remote D1 database bound in `wrangler.jsonc`. | D1 reachability is a smoke, not a replacement for broader production monitoring. |
| `scripts/edge-live-smoke` | Pass | Authenticated deployed worker smoke passed with current and next edge secrets: forged secret rejected, current/next secret enqueue accepted, duplicate idempotency preserved, offline D1 persistence drained into local SQLite and acked after local persistence, expired lease retried, nack-on-local-persist-failure avoided remote ack, and source rate limiting returned `429`. | Uses synthetic staging events; live Telegram webhook is separate. |

## Product Reality

| Area | Exact state | Live status | Tests | Owner | Blocker / next proof |
| --- | --- | --- | --- | --- | --- |
| Local Rust binary | Real CLI with profile, memory, wiki, source card, research, X, worker, backup, cost, secret, cursor, MCP, and HTTP subcommands. | Local only. Build/test verified. | Rust tests. | `crates/arcwell-cli`, `crates/arcwell-core` | Needs installer, service lifecycle, stricter HTTP errors/auth, and release packaging. |
| SQLite store | Real schema and WAL-backed local store. | Local only. | Rust tests cover many flows. | `crates/arcwell-core` | Needs migration discipline beyond schema version 1, vacuum/size checks, broader corruption recovery story. |
| MCP server | Hand-rolled stdio JSON-RPC server with tools/resources. | Works in Rust tests and process-level local stdio smoke; not proven inside authenticated Codex/Claude clients in this audit. | MCP unit tests plus `scripts/claude-mcp-smoke`. | `crates/arcwell-cli`, hosts/plugins | Needs MCP Inspector/real client conformance, cancellation/progress behavior, resource/template maturity. |
| Codex plugin | 99 slash prompts, 11 skills, memory lifecycle hook config, stable plugin, generated dev plugin workflow, and repeatable catalog/docs verifier. `arcwell-codex-dev@arcwell-local` is installed and synced locally. | Stable/dev packages exist; dev install/sync/smoke verified. | Dev wrapper/cache smoke; plugin/docs verifier plus adversarial self-test; no fresh-thread in-app command smoke yet. | `plugins/arcwell-codex`, `.agents/plugins`, `scripts/arcwell-dev` | Needs fresh Codex thread hook/command smoke and command-by-command smoke matrix. |
| Claude host | README, MCP config examples, manual-use guidance, and local stdio validation smoke exist. | Local MCP process validated; degraded/manual mode only in host docs. | `scripts/claude-mcp-smoke`. | `hosts/claude`, `scripts/claude-mcp-smoke` | Needs authenticated Claude Desktop/Code MCP validation. No hooks/skills parity assumed. |
| HTTP server | `/health`, `/profile`, `/memory`, `/wiki`, `/ops` JSON endpoints plus read-only `/ops/ui`. Handlers return structured redacted errors, support optional local bearer or `x-arcwell-http-token` auth, reject hostile browser origins and explicit POSTs, and enforce URI/body/query limits. | Local development only. | Severe HTTP and ops UI tests pass in `crates/arcwell-cli/src/main.rs`; full `cargo test --all --all-features` passes. | `crates/arcwell-cli` | Needs authenticated product ops controls only after specific actions are designed and policy/idempotency coverage is added. |
| Ops visibility | `ops_snapshot`, `/ops`, MCP resources, and read-only `/ops/ui` return durable state for health, backups, worker heartbeat, jobs, dead letters, edge events, watch/source health, cursors, provider/secret health, costs, projects, channels, memory/procedure candidates, work runs, policy decisions/approvals, Telegram delivery failures, and project status snapshots. | Local JSON/HTML only. | Severe ops UI XSS/auth/origin/repeated-POST tests in `crates/arcwell-cli/src/main.rs`; browser desktop/mobile smoke recorded for read-only rendering. | `packages/arcwell-ops`, core, CLI | Needs filtering, sorting, detail drawers, retry/requeue/cancel controls, watchdog summaries, charts, and live provider probes. |
| Service supervision | Worker loop command, worker heartbeat, macOS LaunchAgent install/status/restart/logs/uninstall commands, and `doctor --strict` exist. Strict doctor checks backup verification/freshness, dead letters, heartbeat freshness, required directories, schema drift, LaunchAgent plist presence, corrupt plist metadata, and missing worker binary. | macOS user launchd live smoke passed on 2026-06-20 with disposable paths and killed-worker recovery. Linux systemd is explicitly deferred. | Worker unit tests, strict doctor tests, LaunchAgent plist escaping/clamp test, service plist contract tests, `scripts/service-live-smoke`. | packaging/ops | Needs Homebrew/release packaging and a Linux systemd implementation once a Linux user-service runner is available. |
| Backup | Create, verify, and restore local backup snapshots. Backup creation checkpoints WAL before copying SQLite and copies wiki pages plus Arcwell Memory artifacts. Strict doctor rejects stale or unverifiable latest backup. | Manual only. | Severe backup tests cover wiki page inclusion, memory artifact restore, tamper detection, missing files, manifest path traversal, manifest versioning, stale backup, non-empty target refusal, and full restore drill. | core/ops | Scheduled/off-machine/encrypted backup remains missing. |

## Package Matrix

| Package | Status | Exact state | Tests | Live status | Owner | Blocker / next action |
| --- | --- | --- | --- | --- | --- | --- |
| `arcwell-profile` | Partial | Profile key/value CRUD, search/list/delete through CLI/MCP; stores operating preferences separately from memory. | Profile round-trip, validation, SQL injection-style parameterization tests. | Local SQLite only. | `crates/arcwell-core`, `packages/arcwell-profile` | Needs schema for style/profile sections, import/review workflow, conflict/history, and agent recall discipline. |
| `arcwell-memory` | Done for current local lifecycle; Partial for live/model/UI | The former `mem0-rs` codebase is vendored and renamed as in-repo Arcwell Memory crates. CLI/MCP expose add/search/update/delete/history/forget against the Arcwell Memory provider. Reviewable candidates carry ADD/UPDATE/DELETE/NONE operations, provenance, user scope, sensitivity, applied results, confidence/reason metadata, review-required flags, and rejection reasons. Recall/capture/dream/forget lifecycle events are stored and exposed through MCP/ops. The decision ledger records extraction decisions; `arcwell memory eval-corpus` covers false positives, sensitive medical/secret review, and prompt-injection-as-data. Dream/reconcile removes exact duplicates across provider and compatibility stores and creates reviewable conflict candidates. Forget cascades through active provider vectors, provider history, candidates, compatibility rows, lifecycle inputs, and user-scoped decision observations, then writes a hashed-user tombstone that states retained backups are not rewritten. | Arcwell Memory provider/vector/server tests; severe lifecycle add/search/update/history/delete/forget; env precedence; memory artifact backup restore; provider-backed candidate update/delete tests; sensitive capture review test; false-positive eval corpus; prompt-injection secret review; reviewable conflict update test; recall context test; provider/compat dream tests; active-store forget cascade and tombstone test. | Local provider works with mock or OpenAI config; live OpenAI-backed extraction quality and live Codex/Claude hooks are not freshly E2E verified. | `crates/arcwell-memory*`, `crates/arcwell-core`, `packages/arcwell-memory` | Needs live model-backed extraction quality proof, confidence aging/stale preference review, human review UI, retained-backup erasure/rotation implementation, and live host-hook validation. |
| `arcwell-conversation-import` | Partial | Claude JSON import can dry-run or create review candidates. Raw transcripts are not silently injected. | Candidate tests; no broad real-export corpus test recorded in fresh run. | Local files only. | core/import package | Needs robust Claude/Codex export parsers, model-backed extraction, privacy redaction, large-file streaming, import ledger, resume/idempotency. |
| `arcwell-llm-wiki` | Partial | Markdown page ingest, directory ingest, Codex Swift source import, versioned/auditable source cards, FTS search, watch-source registry, hardened URL ingest, source health, trust-labeled source renderers, generated/source-card artifact evidence exclusion, and URL/RSS/GitHub/arXiv/X source-card paths. | Wiki ingest/search, FTS, source card, adapter parser, unsafe URL/path tests, URL redirect/content-type/size/prompt-injection tests, source trust renderer tests, source-card schema/audit tests, duplicate canonical source-card tests, generated-page evidence exclusion tests, and cursor partial-write/retry-storm tests. | Local durable owner works; Cloudflare polling not live. | core/wiki package | Needs hybrid/vector search, browser-rendered readability extraction, source reliability scoring, provider pagination/rate-limit handling, scheduled polling, and page synthesis. |
| `arcwell-deep-research` | Partial | Research plans, provider web search adapters, tracked role tasks, wiki-grounded brief generation with evidence audit sections, run history, `research_audit`, deterministic contradiction/staleness/untrusted-source checks, and generated/model-answer exclusion from primary evidence. | Research validation, generated-brief/generated-page/source-card-artifact exclusion, fake citation/model-answer audit tests, conflicting launch-date tests, stale/SEO/prompt-injection source audit tests, workflow round trip, provider guard tests, and source trust prompt-injection renderer tests. | Local + optional API keys; host-native search still expected. | core/research package, host skills | Not fully deep. Needs model-backed synthesis/extraction, source triangulation, native OpenAI/Claude search discipline, clustering, and Brave/Perplexity expansion tests. |
| `arcwell-x` | Partial/Risk | X JSON import, OAuth URL/exchange/refresh, recent search, cursors, definitive watch-source rebuild from bookmarks/recent follows, watch-source monitor, report/list. Recent search and monitor check policy/cost before network; token/tier/quota failures are classified and redacted; definitive rebuild swaps watch sources only after candidate collection; the monitor imports watched-source tweets into source cards/wiki pages and digest candidates with safe cursor/source-health behavior. | OAuth, refresh-failure redaction, recent search, cursor, import, unsafe URL, watch rebuild, monitor prompt-injection/digest, quota, malformed/partial response, duplicate newest-id, and budget-blocking tests; live recent-search portion of `scripts/x-live-smoke` reached X with the available bearer token. | Current token is application-only and cannot prove bookmarks/follows watch rebuild or monitor. | core/X package | Needs OAuth user-context token/scope for current account watch rebuild, real quota/tier behavior, Cloudflare callback, cron, and delivery. |
| `arcwell-edge-inbox` | Partial/Risk | D1-backed Cloudflare worker persists events, supports lease/ack/nack/list, Telegram webhook normalization, fixed-window rate limits, and current/next secret rotation. Worker lease semantics now respect nack retry delay, skip exhausted failed rows before later valid work, and honor short staging leases for live retry proof. | Local edge severe tests; worker typecheck plus Node in-memory/D1-fake worker tests; Rust remote-drain tests prove ack-after-persist and nack-on-local-persist-failure; deployed health/D1/forged-secret checks pass; `scripts/edge-live-smoke` passed against the deployed worker. | Synthetic edge live proof passes; live Telegram webhook through the edge remains separate. | edge package, core | Add Miniflare only if future deployed D1 behavior diverges again; replace the simple fixed-window limiter if production abuse patterns require it. |
| `arcwell-telegram` | Partial/Risk | Worker webhook normalization, local drain to channel messages, outgoing send with explicit chat send authorization, MarkdownV2 escaping, delivery attempts, retry hints, token-safe transport-error classification, project binding authorization, conservative authorized natural project resolution, and `scripts/telegram-live-smoke` for disposable local/live proof. | Worker Telegram tests, Rust drain/send/authz/routing/delivery/token-redaction severe tests. | Local auth smoke is scripted; not live-smoked with real Telegram bot/webhook in this audit. | Telegram package, edge inbox, channel model | Needs live Telegram smoke, richer media/update support, automatic delivery retry worker, and real chat context carryover. |
| `arcwell-email` | Scaffold/Partial | Package boundary, Cloudflare Email Routing inbound decision, Gmail host-native boundary, normalized email metadata mapper, source-card/channel-message draft mapping, sender/route authorization rules, attachment/body/link safety policy, and librarian digest-delivery boundary are documented. | Package-local severe Node fixtures cover spoofed `From:`, DMARC failure, malicious HTML/script/CSS, prompt injection as data, attachment bombs, tracking links, duplicate `Message-ID`, oversized bodies, auto-responder hazards, and unauthorized routes. | Local mapper only; no live Cloudflare Email Routing Worker, raw MIME parser, Rust drain/persistence path, Gmail API polling, or outbound email delivery. | `packages/arcwell-email`, edge/channel future | Implement bounded Email Routing worker handler, raw MIME normalization, local drain to durable source cards/channel messages, ops visibility, and disposable live email smoke before claiming ingestion is live. |
| `arcwell-projects` | Partial | Project registry, aliases, ambiguity detection, conservative context follow-up resolution, channel binding, channel authorization policy, manual timestamped status snapshots, status-report envelope with timestamp/source/confidence/provenance, explicit live-state unavailable matrix for Codex/Claude, work-run evidence consolidation into project status, and CLI/MCP manual sync fallback. | Project ambiguity/channel injection tests; severe project live-state/auth tests; Telegram auth/routing tests; MCP round trip. | Local static/manual/evidence-derived registry only; live Codex/Claude thread inventory is explicitly unavailable. | core/projects package | Needs real Codex/Claude thread inventory, running-state summaries, chat context carryover, and project create/update audit trail. |
| `arcwell-work-memory` | Partial | Work runs, events, artifacts, and links are stored in SQLite with redaction, bounded payloads, manual CLI/MCP start/event/artifact/link/finish/search/read/consolidate commands, basic ops listing, and project-status consolidation from trace evidence. Successful runs require validation evidence, and generated summaries cannot be the only consolidation evidence. | Severe work-run tests cover secret redaction, prompt-injection logs as data, malformed host/thread ids, missing validation, generated-summary citation loops, huge payload bounds, search/read, source-card links, and MCP round trip. | Local/manual only; no automatic Codex/Claude lifecycle hooks or scheduled consolidation yet. | core/projects/procedural substrate | Needs host lifecycle hooks, scheduled consolidation, richer ops UI for stale runs/follow-ups, and live host smoke. |
| `arcwell-procedures` | Partial | Reviewable procedural memory exists as dedicated procedure candidates, approved procedures, versioned Markdown artifacts, and CLI/MCP search/read/apply/reject/curate operations. Deterministic extraction from successful validated work runs uses reusable lessons while preserving tool/source/channel text as provenance data. Applying candidates checks policy, auto-approval fails closed by default, sensitive-source auto-approval attempts remain pending, artifacts are written under `ARCWELL_HOME/procedures`, and pending/active procedures are visible in the read-only ops UI. | Severe procedure tests cover prompt injection in traces, malicious tool output as provenance, hostile/path-like titles, overlong method rejection, stale update safety, sensitive-source auto-approval blocking, duplicate curator archive candidates, MCP round trip, and ops UI escaping. | Local/manual only; no model-backed extraction, automatic host hook, plugin retrieval, skill export, or live Codex/Claude procedure retrieval smoke. | core/procedures | Needs explicit confidence/freshness fields, model-backed extraction/evals, richer stale/merge curation, plugin prompts, and reviewed skill export. |
| `arcwell-librarian` | Scaffold/Partial | Rule-based digest candidates and topic expansion from existing wiki/source cards with generated/model-answer evidence exclusion and deterministic evidence audit notes. | Librarian/digest pipeline test plus shared research/source-card audit tests. | Local deterministic only. | core/librarian package | Needs clustering, richer contradiction detection, model-backed page synthesis with citations, proactive alerts, Telegram/email routing. |
| `arcwell-ops` | Partial | `ops_snapshot`, HTTP `/ops`, MCP resources, and read-only `/ops/ui` expose health/jobs/events/cursors/source-health/projects/project-status/channels/deliveries/work-runs/procedures/policy/costs/secret-health. Secret health exposes presence/scope/provider/expiry warnings without values. | Indirect ops/MCP tests plus source-health cursor/partial-write and secret-health redaction tests; severe ops UI escaping/auth/origin/repeated-POST tests. | Local JSON/HTML only. | ops package, CLI | Needs health scoring, queue age summaries, failed-source controls, manual requeue/cancel, filters/detail drawers, and live provider probes. |
| `arcwell-codex` plugin | Partial | Commands and skills exist; command catalog documented. A generated `arcwell-codex-dev` package supports local debug-binary development and cache sync. `scripts/verify-codex-plugin-docs` now checks stale commands, missing MCP references, tracked CLI-only references, untrusted source/channel guards, and README status badges. | Dev install/sync/smoke verified; plugin/docs verifier and self-test pass; no fresh-thread in-app command smoke yet. | Local stable/dev installs exist. | plugin package | Needs fresh Codex thread hook/command smoke and command-by-command smoke matrix. |
| `hosts/claude` | Partial | Documentation for using Arcwell via MCP, degraded-mode expectations, Claude Desktop/Code config examples, manual tool-use prompts, and repeatable local stdio validation. | `scripts/claude-mcp-smoke` covers local stdio discovery, malformed frames, unsupported methods, bounded large responses, bad/missing `ARCWELL_HOME`, and unavailable lifecycle hooks. | Not freshly validated in real Claude profile; local config lacked `mcpServers.arcwell`. | host adapter docs | Needs actual Claude Desktop/Code MCP UI test and MCP Inspector run against `arcwell mcp`. |

## Feature Matrix

| Feature | Status | Exact state | Tests | Live status | Owner | Blocker / todo |
| --- | --- | --- | --- | --- | --- | --- |
| Profile CRUD | Done for MVP | Set/get/search/list/delete. | Yes. | Local only. | profile/core | Add history/conflict tracking. |
| Personal memory CRUD | Partial | Arcwell Memory add/search/update/delete/history/forget exists; simple SQLite rows remain as compatibility path. | Yes, including severe lifecycle and restore tests. | Local only; OpenAI-backed live provider not freshly E2E verified. | memory/core | Wire reviewed extraction/candidates and UI/agent recall discipline to Arcwell Memory. |
| Memory extraction | Partial | Deterministic extraction creates reviewable ADD/UPDATE/DELETE/NONE candidates and apply uses Arcwell Memory provider operations. Direct provider inference can be requested for capture, but the model-backed extractor/eval loop is not complete. | Narrow yes, including provider-backed update/delete and sensitive auto-apply refusal. | Local only; live provider extraction not freshly E2E verified. | memory/core | Model extraction, scoring, review UI, false-positive evals, richer conflict detection. |
| Memory dream/reconcile | Partial | Exact duplicate provider memories are auto-deleted, compatibility duplicates are cleaned, compatibility rows that duplicate provider memories are removed, and provider same-subject conflicts create reviewable delete candidates. | Yes, including severe provider duplicate/conflict and compatibility duplicate tests. | Local only. | memory/core | Model synthesis, confidence aging, stale preference review, scheduling, and richer merge policies. |
| Candidate review | Partial | List/apply/reject candidates with operation, memory id, user id, metadata, sensitivity, confidence/reason metadata for memory decisions, applied result, applied timestamp, and rejection reason. | Yes. | Local only. | core | Better UI and broader provenance display remain missing. |
| Conversation import | Partial | Claude import to candidates. | Light/indirect. | Local files only. | conversation-import | Real export corpus tests, streaming, redaction, Codex import. |
| Wiki Markdown ingest | Partial/Done for MVP | File/dir ingest into Markdown pages + FTS. | Yes. | Local only. | wiki/core | Better metadata, incremental sync, deleted-file handling. |
| Wiki source cards | Partial | Typed source cards write Markdown wiki artifacts with schema version metadata, evidence role, trust level, extracted dates/entities, quality flags, canonical dedupe, and untrusted-evidence framing. | Yes, including malformed schema, too-many claims, hostile source text, and generated-recursion coverage. | Local only. | wiki/core | Formal JSON Schema export, richer claim extraction, and source reliability scoring. |
| URL ingest | Partial/Risk | HTTPS URL ingest validates redirects, content type, size bounds, and canonical URL, then writes `UNTRUSTED_SOURCE_EVIDENCE` Markdown with provenance, cleaned readable text, and escaped source excerpt. | Unsafe URL tests plus severe redirect-to-private, binary/wrong-type, huge body, canonical duplicate, prompt-injection-as-data, and Markdown/HTML escaping tests. | Live HTTP possible. | wiki/core | Browser-rendered readability, robots/crawl-rate policy, source reliability scoring, and slow-response/crawl politeness remain. |
| RSS/Atom adapter | Partial | Fetches/parses feed, writes deduped source cards, records item-level cursor/source health after durable writes. | Parser/unsafe-link tests plus cursor partial-write and retry-storm dedupe tests. | Live possible. | wiki/core | Provider rate-limit handling and scheduled polling remain. |
| GitHub adapter | Partial | Owner/repo releases/commits and owner repos fetch to deduped source cards with cursor/source-health success/failure state. | Mapper/path injection tests plus shared source-card dedupe/cursor health coverage. | Live possible. | wiki/core | Pagination, ETags, rate handling, org/team source registry, richer repo event semantics. |
| arXiv adapter | Partial | Query API fetches deduped source cards and records submitted date/id cursor/source-health state. | Parser tests plus shared source-card dedupe/source-health coverage. | Live possible. | wiki/core | Provider backoff/rate handling, paper PDF/fulltext handling, and scheduled polling remain. |
| Watch sources | Partial | Registry exists; Codex Swift source import and X rebuild write sources. | Watch validation tests. | Local config only. | wiki/X/core | Scheduler, health, per-source budgets, UI, owner import audits. |
| Worker jobs | Partial | Queue, leases, retry, dead-letter, run-once/run loop. | Severe lease/failure/unknown-kind tests. | Manual process only. | core/ops | Requeue/cancel, supervisor, heartbeat, policy-specific backoff. |
| Edge inbox local table | Partial | Enqueue/lease/ack/nack/dead-letter with TTL/idempotency. | Severe tests. | Local only. | edge/core | Cloudflare durable counterpart and drain sync. |
| Edge Worker | Partial/Risk | D1-backed worker validates secret/payload/idempotency/TTL, persists events, supports lease/ack/nack/list, accepts current/next secrets, and rate-limits by source/chat. | Typecheck, worker tests, deployed health/D1 smoke, and `scripts/edge-live-smoke`. | Synthetic deployed edge proof passes; live Telegram webhook proof remains. | edge worker | Production monitoring and richer abuse controls remain. |
| Channel model | Partial | Generic message records with direction/project/source event, authorization policy, delivery attempts, and `UNTRUSTED_CHANNEL_EVIDENCE` rendering for agent-facing display. | Channel injection/auth/delivery tests plus source trust evidence-renderer tests. | Local only. | projects/telegram/core | Richer formatting policies per channel and automated delivery retry worker. |
| Telegram incoming | Partial/Risk | Worker `/telegram/webhook` normalizes text/caption updates into edge events; local `telegram drain` records Telegram events, enforces project-binding authorization, and can auto-bind authorized uniquely resolved project references. `scripts/telegram-live-smoke` documents the real webhook drain proof. | Worker tests plus Rust severe drain/auth/routing tests. | Not deployed/live-smoked with Telegram in this audit. | telegram/edge/core | Live webhook deploy, richer media/update support, and real follow-up context carryover missing. |
| Telegram outgoing | Partial/Risk | `telegram send`/MCP sendMessage path requires explicit chat send authorization, escapes MarkdownV2, calls Telegram API, records outgoing channel message, and persists delivery attempts with failed status/retry hints while avoiding bot-token leakage in transport errors. | Mocked Rust success, unauthorized-send, 429 failure, retry, and transport-error token-redaction tests. | Not live-smoked with Telegram bot in this audit. | telegram/core | Automatic retry worker, richer send types, and live test missing. |
| Project registry | Partial | Name/aliases/status/summary, resolver, timestamped status snapshots with source/thread provenance, status reports with explicit live-state availability, channel-scoped read authorization, and CLI/MCP manual sync fallback. Work-run consolidation can write evidence-derived status snapshots. | Yes, including severe stale/manual-as-live, forged channel, ambiguous follow-up, missing thread ref, and prompt-injection-as-data tests. | Local static/manual/evidence-derived registry; live Codex/Claude thread inventory unavailable by explicit report. | projects/core | Real Codex/Claude thread inventory and automatic live summaries missing. |
| Work-memory graph | Partial | Compact work traces can record goal, project id, host/thread ids, agent surface, events, artifacts/files, links to source cards/wiki/memory/cost/backup/work runs/generated summaries, failures/root causes, validation, outcome, follow-ups, and reusable lessons. Search/read and consolidation to project status proposals are exposed through CLI/MCP. | Yes, including severe redaction, prompt-injection-as-data, malformed ids, validation-required success, generated-summary loop prevention, huge payload bounds, and MCP round trip. | Local/manual only. | core/work-memory | Automatic host hooks, scheduled consolidation, and richer ops/follow-up views remain missing. |
| Procedural learning | Partial | Completed validated work runs can propose pending procedure candidates; reviewed candidates can create, update, archive, search, and read versioned procedures; exact duplicate titles can produce reviewable archive candidates. Procedures are procedural memory, not personal memory or factual source evidence. Pending/active procedures are visible in the read-only ops UI. | Yes, including severe prompt-injection, malicious tool-output provenance, title/path traversal, overlong text, stale update, sensitive-source auto-approval, curator duplicate, MCP round-trip tests, and ops UI escaping coverage. | Local/manual only. | core/procedures | Needs model-backed extraction/evals, explicit confidence/freshness policies, richer stale/merge curation, scheduled extraction, plugin retrieval, and skill export. |
| Project switching from chat | Partial | Telegram payloads can bind explicit `projectId` only for authorized subjects; authorized chats can auto-bind uniquely resolved project references from message text. Channel-scoped status reads can require `can_read_projects`. Ambiguous "other project" style follow-ups fail closed without stronger channel context. | Telegram auth/routing tests plus severe project status auth/follow-up tests. | Not live in Telegram. | projects/telegram | Context carryover for follow-ups and live channel testing missing. |
| Codex thread awareness | Missing/Scaffold | Project status snapshots can store Codex thread refs manually, and status reports explicitly say Codex live inventory/read is unavailable unless a future adapter verifies it. | Severe project live-state tests cover host-labeled snapshots, stale snapshots, and missing/deleted thread refs as unavailable. | Not live. | projects/host | Need Codex thread APIs or app integration. |
| Claude thread awareness | Missing/Scaffold | Project status snapshots can store Claude thread refs manually, and `hosts/claude` documents the degraded capability matrix. Claude lifecycle hooks remain unavailable/unproven. | Degraded-mode docs plus severe generic live-state tests. | Not live. | hosts/claude | Real Claude MCP/client validation and any future lifecycle/thread API proof. |
| Librarian digest candidates | Partial | Rule-based score/reason from source cards. | Yes. | Local only. | librarian/core | Interestingness model, clustering, alert routing. |
| Librarian page expansion | Scaffold/Partial | Creates deterministic expanded page from local search/source cards, excludes generated/model-answer source cards as primary evidence, and includes evidence audit notes. | Yes. | Local only. | librarian/wiki | Model synthesis, richer citations, clustering, and source expansion fanout. |
| Proactive alerts | Missing | Digest candidates exist and the email delivery boundary is documented, but no delivery to Telegram/email exists. | None for delivery. | Not live. | librarian/channels | Delivery routing, schedule, thresholds, quiet hours, dedupe, policy/cost checks, and delivery attempts. |
| Research plans | Partial | Produces local plan/suggested searches. | Yes. | Local only. | research/core | Host-native tool wiring and evals. |
| Research provider search | Partial | Brave/OpenAI/Perplexity adapter guardrails exist. | Guard tests. | Live possible with keys; not freshly E2E verified. | research/core | Native search preference, provider-specific live tests, citation quality checks. |
| Research workflow tasks | Partial | Creates role tasks and completion notes. | Yes. | Local only. | research/core | Actual subagent orchestration remains host-driven. |
| Research brief | Scaffold/Partial | Wiki-grounded deterministic brief with source-card evidence audit, generated/model-answer exclusion, stale/untrusted/contradiction findings, and source-card artifact recursion prevention; still not true model synthesis. | Yes, including fake citation/model-answer, generated recursion, conflicting launch date, stale source, SEO spam, and prompt-injection tests. | Local only. | research/wiki | Model-backed synthesis, skeptical pass beyond deterministic heuristics, richer evidence tables, and live freshness gates. |
| X OAuth | Partial | PKCE URL, exchange, refresh, SQLite tokens with stored expiry metadata when provider responses include `expires_in`; expired local tokens and refresh endpoint failures are surfaced without token values. | Yes, including redacted token storage, expired-token health, and refresh-failure redaction/classification. | Live possible; not freshly E2E verified. | X/core | Cloudflare callback, provider-side revoke/rotation proof, and live scope validation. |
| X recent search | Partial | Live API recent search, cursor state, pre-network policy/cost guard, X HTTP error taxonomy for token/tier/quota responses, and cursor no-advance on partial/malformed provider responses. | Mocked/guarded tests plus live recent-search portion of `scripts/x-live-smoke` with the available bearer token. | Live recent search works for the tested query/token; broader tier/quota behavior is still limited by provider plan. | X/core | Broader timeline/list adapters and live quota/tier matrix. |
| X definitive watch list | Partial | Rebuilds `x_handle` watch sources from bookmarks and capped recent follows; polluted prior lists are replaced only after provider candidate collection succeeds and SQLite swap commits. | Yes, including polluted-list and provider-failure preservation tests. | Live attempt with application-only bearer token failed safely with provider `403 Unsupported Authentication`. | X/core | Needs OAuth user-context token/scope for bookmarks/follows account data. |
| X production monitor | Partial/Risk | `x monitor-watch-sources` / `x_monitor_watch_sources` polls the active definitive `x_handle` watch list, imports accepted tweets into source cards/wiki pages, creates digest candidates, records `x:watch:<handle>` cursors and source-health, and fails safely for quota/tier/token/partial/malformed responses. | Severe tests cover expired token, refresh failure, quota/rate-limit, protected/deleted partial errors, malformed tweets, prompt injection as evidence, duplicate newest-id, and digest/source-card flow. | Local monitor logic is proven; live recent search works with the available bearer token, but watch rebuild/monitor live proof is blocked by missing OAuth user-context account-data auth. | X/core | User-context live smoke, model-backed interestingness, alert delivery, Cloudflare callback/cron. |
| X reporting | Scaffold/Partial | Local list/report over imported items plus digest candidates from watched-source monitor imports. | Indirect. | Local only. | X/core | Model-backed interestingness, trend summaries, source linking, delivery. |
| Secrets | Partial/Risk | SQLite secret values and refs; lists hide values; `secret_health`, `/ops`, MCP resources, and `doctor --strict` expose missing/expired/scope/provider health without values. Backup manifests mark whether local secret values are present without enumerating values. | Severe tests cover list/MCP/ops/resource redaction, expired values, missing configured refs, provider failure/job-error redaction, command echo, and backup manifest sensitivity. | Local only; provider metadata only unless a specific live smoke is run. | core/secrets | Needs broader provider-specific live probes, provider-side revocation helpers, and scheduled rotation reminders. |
| Cost tracking | Done for current local scope | Manual/estimated cost entries, summary, durable global/package/provider/source policies, kill switches, temporary overrides, atomic estimated-cost reservations, durable cost decisions, and budget gates before current Arcwell-owned paid/provider/network paths: daemon web search, X recent/OAuth/following/bookmark paths, Arcwell Memory OpenAI provider setup, Telegram send/retry, remote edge drain, and scheduled URL/RSS/GitHub/arXiv/X network jobs. | Severe budget tests cover negative/NaN/infinite/huge cost rejection, cap overrun, reservation race prevention, retry-storm overspend prevention, kill switch before scheduled HTTP, override expiry, pre-credential network blocking, and ops/cost visibility for blocked jobs. | Local/mock only; live provider billing not reconciled. | core/cost | Needs provider-reported actual-cost reconciliation where APIs expose usage, live-provider smoke, and product-grade ops UI burn-down charts/controls. |
| Policy engine | Partial | `ARCWELL_HOME/arcwell-policy.toml` supports `[[rules]]` with allow, deny, require_approval, and defer decisions over action/package/provider/source/channel/subject/target fields. Decisions and pending approvals are stored in SQLite and surfaced in ops snapshots. Deny/approval guards run before X recent search, X monitor, daemon web search, memory candidate apply, procedure candidate apply, Telegram send/retry, and local project writes; existing cost checks still run after policy allow and before credentials/network calls. | Severe policy tests cover malformed policy fail-closed behavior, provider/network denial before credentials or mutation, memory/Telegram/project deny audits, approval creation, stale/broad wildcard precedence, and hostile payload snippets stored as data. Procedure severe tests cover auto-approval fail-closed behavior. | Local in-process only. | core/policy | Coverage is not yet universal: no CLI/MCP policy admin tools, no source-ingestion/secret-access/worker/memory-capture policy guards, no OS/kernel sandboxing, and no approval-resolution workflow. |
| Doctor | Partial | `doctor --strict` fails on missing/unverifiable/stale backups, stale/missing heartbeat, dead letters, missing required directories, schema drift, missing/non-file LaunchAgent plist when configured by the CLI, corrupt service metadata, missing worker binary, and health warnings such as expired/missing credentials. | Severe strict-doctor, service plist contract, and secret-health tests. | Service supervision live smoke passed for macOS launchd; live provider probes remain unproven. | cli/ops | Linux systemd and live provider probe evidence still missing. |
| Ops UI | Partial | Read-only server-rendered `/ops/ui` shows the durable ops snapshot across health, backups, worker heartbeat, queues, edge events, watch/source health, cursors, provider/secret health, costs, projects, channels, memory/procedure review, work runs, policy decisions/approvals, Telegram delivery failures, and project status snapshots. Mutating controls are intentionally absent. | Severe XSS tests cover channel text, source card title/body, project name/status, procedure title, work run text, policy denial reason, and error text; auth/origin/repeated POST tests keep the read-only stance fail-closed. | Local browser smoke only. | ops | Needs filters, detail drawers, charts, and narrow authenticated policy-checked controls. |
| Packaging | Partial | macOS service install/status/restart/logs/uninstall commands exist; release/readiness checklist and smoke exist; stable plugin targets installed `arcwell` on `PATH`; dev-loop tooling generates and syncs `arcwell-codex-dev` for Codex app development. | LaunchAgent plist escaping/clamp test; strict doctor service-plist test; service plist contract tests; `scripts/service-live-smoke`; `scripts/release-readiness-smoke`; `scripts/arcwell-dev smoke/sync`. | Not published through Homebrew/GitHub release artifacts; macOS launchd live smoke passed; fresh-thread Codex dev plugin smoke remains; Linux systemd is deferred. | packaging | Homebrew formula/tap, signed/checksummed release artifacts, checksum-verifying installer, fresh-thread plugin smoke, and Linux systemd implementation when a Linux user-service runner is available. |
| Cloudflare deploy | Partial/Risk | Worker package exists with D1 binding in `wrangler.jsonc`; deploy to `https://arcwell-edge-inbox.chabotc.workers.dev`, deployed health, forged-secret rejection, remote D1 access, and authenticated ingress/drain smoke pass. | Typecheck, worker tests, deployed health/forged-secret smoke, remote D1 `SELECT 1`, secret-name listing, and `scripts/edge-live-smoke`. | Live Telegram webhook and broader production monitoring remain. | edge/package | Keep current/next secret rotation discipline and add production monitoring if this becomes user-facing critical path. |
| Garderobe integration | Partial/Risk | `packages/arcwell-garderobe` vendors the Garderobe Cloudflare Worker, D1 migrations, OAuth/DCR MCP server, admin UI, and outfit/wear/rotation tools as a first-class package boundary. Private seed SQL, `.dev.vars`, Wrangler state, node modules, and live-remote severe scripts are excluded; deploy config uses placeholder D1/KV ids. Arcwell memory/profile/wiki boundaries are documented as opt-in for private inventory sync. | Package-local severe integration test checks auth/DCR/PKCE wiring, copied-secret/seed absence, hostile wardrobe metadata handling, weather fallback docs, and default no-sync into memory/wiki; TypeScript typecheck passes locally. | No fresh Arcwell-owned Cloudflare deployment, live Claude/host OAuth handshake, or disposable wardrobe MCP E2E from this package. Adjacent source project lacks explicit top-level license file, so public redistribution remains blocked. | `packages/arcwell-garderobe`, docs | Provision D1/KV, set secrets, run live remote MCP smoke with disposable fixture rows, settle license/provenance, and add host connector evidence. |
| Google Workspace | Strategy decided | Host-native Google connectors first; Arcwell only stores selected Workspace metadata/content when explicitly archived into projects, wiki, source cards, work runs, or review queues. `docs/google-workspace-strategy.md` defines the permission matrix and non-goals. | Documentation-level severe review covers overbroad scopes, stale calendar state, private content leakage, prompt injection, and non-owner channel access. | Available through connected host tools; no Arcwell Google API package or live connector smoke in this repo. | future package/host | Implement only narrow `arcwell-workspace-context` capture/index flows if a concrete connector gap appears. |
| Email ingestion | Scaffold/Partial | `arcwell-email` defines the package boundary, chooses Cloudflare Email Routing for Arcwell-owned inbound capture, keeps Gmail host-native for selected mailbox work, and provides a tested normalized mapper to email edge events, source-card drafts, and channel-message drafts. | Package-local severe mapper fixtures. | Not live. | email/channel/edge | Needs Cloudflare Email Routing Worker, bounded MIME normalization, Rust drain/persistence, ops visibility, and disposable live smoke. |
| Browser ops UI/json-render | Partial | `/ops/ui` renders local ops HTML from `ops_snapshot`; `/ops` and MCP resources remain the JSON surfaces. | Server-side severe escaping/security tests plus desktop/mobile browser smoke. | Local only. | ops/ui | Needs richer interaction model and controls. |

## Trust Blockers

These are the items that can break user trust even if the happy path appears to
work.

| Priority | Blocker | Why it is dangerous | Required done condition |
| --- | --- | --- | --- |
| P0 | Cloudflare edge synthetic live proof now passes | D1 persistence, drain protocol, auth rotation, local Rust drain sync, and rate limits are implemented in code/tests and passed against the deployed worker with synthetic events. | Live Telegram webhook through the edge and operational monitoring remain. |
| P0 | Service supervision release packaging is still incomplete | The macOS LaunchAgent service and local release readiness are smoke-tested, but Homebrew/GitHub publication, signed/checksummed installers, and Linux systemd are not implemented. | Homebrew/release artifact publication smoke and Linux systemd implementation on a Linux user-service runner. |
| P0 | Telegram exact incoming channel-record proof is not complete | Incoming/outgoing code paths exist with auth, routing, delivery, send-authorization, token-redaction tests, and a disposable live-smoke runner. Live `getMe`, webhook setup, outgoing send, and one incoming deployed-edge webhook event were observed, but the strict exact-message local channel-record assertion timed out. | Re-run `scripts/telegram-live-smoke` with a known exact incoming chat message and preserved temp home on failure. |
| P0 | Project/thread state is not live | "How is X going?" can answer from manual snapshots or work-run trace evidence, and now reports live-state unavailable explicitly, but it still cannot inspect live Codex/Claude threads. | Codex/Claude thread inventory or an explicit verified sync protocol with live-smoked summaries. |
| P1 | Memory lifecycle local scope is complete; live/model/UI proof remains | Arcwell Memory provider-backed candidates, recall/capture tools, lifecycle events, Codex hook config, active-store dream/reconcile, decision ledger, deterministic eval corpus, reviewable conflict updates, and active-store forget cascade with backup tombstones exist. Historical backups are tombstoned but not rewritten; live Codex/Claude hooks and live model-backed extraction quality are unproven. | Fresh Codex/Claude host-hook smoke, live model-backed extraction quality evals, retained-backup erasure/rotation implementation, and human review UI. |
| P1 | Work-memory graph is manual, not host-live | Core work traces and project-status consolidation exist with severe tests, but Codex/Claude lifecycle capture and scheduled consolidation are not wired. | Add host hooks/prompts or explicit sync protocol, live-smoke with disposable threads, and build ops views for stale runs/follow-ups. |
| P1 | Procedural learning is local/manual | Reviewable procedure candidates, versioned artifacts, policy-gated apply, search/read, update/archive, duplicate curation, and read-only ops visibility exist, but extraction is deterministic/manual and not yet wired into host hooks, plugin retrieval, model evals, or skill export. | Add model/eval gate, explicit confidence/freshness, host retrieval, richer stale/merge curation, and reviewed skill export. |
| P1 | Research/librarian can sound smarter than they are | Deterministic local summaries may be mistaken for current, comprehensive synthesis. | Model-backed synthesis with citations, skeptical pass, current search gates, quality evals. |
| P1 | Ops UI/doctor are shallow | Read-only visibility now exists for the main local failure domains, but operators still lack filters, summary health scoring, and safe remediation controls. | Add filtered/detail UI, queue/source/credential summaries, and narrow authenticated controls with policy, CSRF, and idempotency tests. |
| P1 | Cost controls need live/provider-cost proof, not local gate coverage | Current Arcwell-owned paid/provider/network paths reserve estimated spend or block before credentials/network, and blocked scheduled jobs are visible in job, ops, and cost-decision state. Remaining risk is accuracy/completeness of live provider usage accounting and product-grade burn-down controls. | Add provider-reported actual-cost reconciliation where available, live-provider smoke, and ops UI budget burn-down/controls. |
| P1 | Policy enforcement is real but incomplete | Core policy decisions, audits, approvals, and several sensitive guards exist, but many Arcwell-owned actions still do not consult policy and there is no CLI/MCP admin or approval-resolution workflow. | Finish policy guards for source ingestion, secret access, worker jobs, procedure apply, memory capture, and remaining provider paths; add CLI/MCP tools and approval resolution; keep OS sandboxing as a separate non-claim. |
| P1 | Local HTTP hardening is improved, ops UI remains read-only | P1.13 now has result-based handlers, optional bearer/local token auth, cross-origin/POST fail-closed stance, size limits, redacted errors, severe regression tests, and a broader read-only ops UI. | Build richer authenticated ops controls only after concrete actions are scoped; keep mutating routes CSRF-protected, policy-checked, idempotent, and tested. |
| P2 | MCP/client compatibility is under-proven | Hand-rolled MCP now has local Claude-style stdio smoke for malformed frames, unsupported methods, bounded large responses, and host-lifecycle unavailability, but real client UI behavior can still differ. | MCP Inspector test, Codex plugin test, authenticated Claude Desktop/Code test. |
| P2 | Docs can overpromise | Product prose may imply capabilities that are scaffolds. | Status badges in README/package READMEs linked to this file, plus `scripts/verify-codex-plugin-docs` in CI/dev checks. |

## Immediate Work Queue

1. Keep `scripts/verify-codex-plugin-docs` green as plugin commands, skills,
   MCP tools, and package READMEs change.
2. Live-smoke edge inbox on Cloudflare D1 and add local Rust drain sync from the
   deployed worker.
3. Run `scripts/telegram-live-smoke` against a disposable Telegram bot/chat and deployed edge worker, then add automatic retry worker/context carryover.
4. Add filters/detail drawers and narrow authenticated controls to the read-only
   ops UI for queues, failures, source health, credentials, backups, costs,
   work runs, and review candidates.
5. Complete Arcwell Memory model extraction/evals, confidence aging, backup
   forget policy, UI, and live Codex/Claude hook validation.
6. Complete procedural learning retrieval/evals, confidence/freshness,
   stale/merge curation, and reviewed skill export.
7. Add real model-backed librarian/research synthesis with source-grounded
   citations and skeptical review.
8. Wire work/project status to live Codex/Claude state or build an explicit
   sync protocol that does not pretend.
9. Publish Homebrew/GitHub release artifacts only after adding signed/checksummed
   archives or a formula, then implement Linux systemd user-service support on a
   Linux runner; keep those gates separate from local release readiness and
   macOS launchd proof.
10. Finish policy coverage and hard cost controls before enabling broad always-on
   monitoring.

## Ownership Rule

Every future feature should update this file in the same change that implements
it. If a command, MCP tool, package README, or plugin prompt exists before the
capability is actually production-real, it must be marked `Partial`, `Scaffold`,
or `Risk` here.
