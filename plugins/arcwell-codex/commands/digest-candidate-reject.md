---
description: Reject a sourced digest candidate
argument-hint: [candidate-id]
---

Use `digest_candidate_reject` for the supplied candidate id. Include the user's reason as `note` when provided. Rejection must be durable review state, not deletion.
