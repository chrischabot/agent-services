---
description: Restore an applied X watch curation run
argument-hint: <run-id>
---

# X Watch Curation Restore

The user invoked this command with: $ARGUMENTS

Require an explicit curation run id, then use `x_restore_watch_curation`. Report restored count, restored watch-source ids, and non-claims. Do not imply restore can recover decisions that have no restore snapshots.
