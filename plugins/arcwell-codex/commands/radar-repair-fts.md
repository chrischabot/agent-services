---
description: Rebuild radar item FTS rows
argument-hint: [--run-id <run-id>]
---

# Radar Repair FTS

The user invoked this command with: $ARGUMENTS

Use `radar_rebuild_fts`. After rebuilding, run `radar_audit_run` for the affected
run when a run id is known.
