---
description: List durable radar delivery attempts.
---

Use `radar_delivery_list`. Optionally filter by `run_id`.

Treat these rows as delivery-attempt evidence only. A blocked or failed row is
not a successful delivery, and a successful manual row is not scheduled or
operational proof.
